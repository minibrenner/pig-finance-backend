# Dependências
# Ignora a pasta onde os pacotes do npm são instalados.
node_modules/

# Logs
# Ignora logs gerados pelo npm em caso de erro.
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Variáveis de Ambiente
# NUNCA envie seus segredos (senhas, chaves de API) para o repositório.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Pasta de build/produção
# Ignora a pasta que será gerada quando prepararmos o app para produção.
dist
build

# Arquivos de Sistema Operacional
.DS_Store
Thumbs.db
/generated/prisma
